# UI De-Slopping Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (` - [x]`) syntax for tracking.

**Goal:** Strip away all "AI slop," robotic commentary, and meta-branding from the UI to achieve a pure, high-signal data engine aesthetic.

**Architecture:** Surgical text replacements across React components and logic files to shorten labels and eliminate filler sentences.

**Tech Stack:** Next.js, React, Tailwind CSS.

---

## Task 1: Clean Dashboard Header & Importer

**Files:**
- Modify: `src/app/page.tsx`
- Modify: `src/app/components/MainImporter.tsx`

 - [x] **Step 1: Remove job-title sub-header in `page.tsx`**
Remove the `<p className="text-zinc-600 text-[10px] uppercase tracking-[0.4em] font-black">Capital Efficiency Analyst</p>` block.

 - [x] **Step 2: Simplify importer message in `MainImporter.tsx`**
Change "Processing Strategy DNA..." to "Processing Holdings...".

## Task 2: De-slop Strategic Discussion (Efficiency)

**Files:**
- Modify: `src/app/components/StrategicDiscussion.tsx`

 - [x] **Step 1: Simplify headers and remove robotic praise**
    - "Efficiency Analysis" -> "Efficiency".
    - Remove the "Agentic Commentary" `<h3>`.
    - Only show the commentary `<div>` if `drag.totalDragBps > 0`. If 0, show nothing.
    - Remove phrases like "Significant drag detected" or "Recommend immediate location swap."
    - New phrasing: `$drag.locationDragBps.toFixed(0) bps leakage from high-yield assets in taxable accounts.`

## Task 3: Clean Risk & Concentration Widget

**Files:**
- Modify: `src/app/components/RiskWidget.tsx`

 - [x] **Step 1: Punchy labeling and remove insights**
    - "Exposure Audit" -> "Concentration".
    - "High Concentration (>5%)" -> "Concentration (>5%)".
    - "Actionable ER Drag" -> "ER Savings".
    - Remove the entire "System Insight" `div` block at the bottom.

## Task 4: Clean Alpha & Rebalancer Logic

**Files:**
- Modify: `src/app/components/AlphaPerformance.tsx`
- Modify: `src/lib/logic/rebalancer.ts`

 - [x] **Step 1: Clean Alpha Performance UI**
    - "Alpha Risk Analysis" -> "Alpha Performance".
    - Remove the entire italicized quote block at the bottom.

 - [x] **Step 2: Simplify Rebalancer reasoning**
In `rebalancer.ts`, remove the sentence: "Reallocating capital improves efficiency." Keep only the data-driven first half.

## Task 5: Clean Macro Insights (Audit Page)

**Files:**
- Modify: `src/app/components/MacroInsights.tsx`

 - [x] **Step 1: Remove predictive fluff**
    - Strip "Historically precedes recessionary risk. Consider increasing short-term liquidity."
    - Strip "Economic momentum may be peaking. Monitor REIT and long-duration exposure."
    - Just show the spread data and a state label (e.g. "INVERTED").
