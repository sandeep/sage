# Sage (The Logic Wrapper) Implementation Plan - Phase 2

> **For agentic workers:** REQUIRED: Use superpowers:executing-plans to implement this plan. Steps use checkbox (` - [x]`) syntax for tracking.

**Goal:** Complete the "Prescriptive Orchestration" workflow by implementing ETF decomposition (Asset X-Ray) and longitudinal rebalance tracking.

**Architecture:** Extend the existing Next.js + SQLite engine to handle fund-of-fund decomposition and historical directive logging.

**Tech Stack:** Next.js, SQLite, TypeScript.

---

## Chunk 1: ETF Look-Through (Asset X-Ray)

**Goal:** Implement the logic to decompose funds (VTI, VXUS) into their underlying security exposure.

### Task 1: ETF Composition Registry ✅

**Files:**
- Create: `src/lib/data/etf_composition.json`
- Modify: `src/lib/logic/xray.ts`

- [x] **Step 1: Create sample composition data.**
- [x] **Step 2: Update `calculateTrueConcentration` to use this data.**
- [x] **Step 3: Verify the "embedded" vs "direct" exposure math in tests.**
- [x] **Step 4: Commit.**

---

## Chunk 2: Directives History & Execution

**Goal:** Track rebalances over time and allow marking tasks as complete.

### Task 2: Rebalance History Schema ✅

**Files:**
- Modify: `src/lib/db/schema.sql`

- [x] **Step 1: Add `executed_at` and `final_value` to the directives table.**
- [x] **Step 2: Update `rebalancer.ts` to skip already-accepted directives during re-calc.**
- [x] **Step 3: Commit.**

### Task 3: Execution UI ✅

**Files:**
- Modify: `src/app/components/TaskBlotter.tsx`
- Modify: `src/app/api/directives/route.ts`

- [x] **Step 1: Implement "Mark as Executed" button.**
- [x] **Step 2: Add a "History" view to see past rebalances.**
- [x] **Step 3: Commit.**
